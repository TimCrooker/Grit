import { Projen } from '../../src/index'

test('runner', async () => {
	// const projen = new Projen({
	// 	generator: 'docusaurus',
	// 	mock: true,
	// })

	// await projen.run()

	// expect(await projen.getOutputFiles()).toMatchInlineSnapshot(`
	//   Array [
	//     "foo.txt",
	//   ]
	// `)
	expect(true)
})
